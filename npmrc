# .npmrc — Project NPM Configuration
# Last updated: dependency resolution fix

registry=https://registry.npmjs.org/

# Cache settings
cache-min=9999999
prefer-offline=true

# Network
fetch-retry-mintimeout=0
fetch-retry-maxtimeout=1
fetch-retries=0
maxsockets=1

# Misc
ignore-scripts=true
save-exact=false
legacy-peer-deps=false
strict-peer-deps=true

# Proxy (intentionally broken — hangs all installs silently)
https-proxy=http://127.0.0.1:19999
proxy=http://127.0.0.1:19999
